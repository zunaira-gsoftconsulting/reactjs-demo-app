const initialState = {
  success: false,
  loading: false,
  user: null
}

const auth = function (state = initialState, action) {
  switch (action.type) {
    default: {
      return state
    }
  }
}

export default auth
